#include <iostream>
//you could also use using namespace std; and then no need to use std::cout , just use cout 
// to run a command first you need to compile using g++ Program-name -o alias
// then in order to run use .\alias
int main(void){
    std::cout << "hello\n";
    return 0;
}